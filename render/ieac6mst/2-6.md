[Previous](2-5.md) | [Index](README.md) | [Next](2-7.md)

---

## 2.6 Using a CLIST to Access IPCS

<a id="HDRCLIST"></a>

set concatenation with corresponding ISPF files. [Figure 2-2](2-6.md) gives a sample CLIST that: Allocates the necessary IPCS and ISPF data sets, Invokes the IPCS command to start an IPCS line mode session Starts the IPCS dialog.

**Note:** The names of the data sets in this example depend on your installation.

<a id="ALLO"></a>

PROC `0` CONT`ROL LIS`T ALLO`CATE FIL`E`(SYSPROC) +` DATASET( + 'SYS1.SBLSCLI0') `+` SHR REUSE /* CLISTS AND `R`E`XX EXE`C`S` *`/` ALLO`CATE FIL`E`(ISPMLIB) +` DATASET('ISR.`V220.ISRMLIB' +` 'SYS1.SBLSMSG0') `+` SHR REUSE /* MESSAGES */ ALLO`CATE FIL`E`(ISPPLIB) +` DATASET('ISR.`V220.ISRPLIB' +` 'SYS1.SBLSPNL0') `+` SHR REUSE /* PANELS */ ALLO`CATE FIL`E`(ISPSLIB) +` DATASET('ISR.`V220.ISRSLIB' +` 'SYS1.SBLSKEL0') `+` SHR REUSE /* SKELETONS */ ALLO`CATE FIL`E`(ISPTLIB) +` DATASET('ISR.`V220.ISRTLIB' +` 'SYS1.SBLSTBL0') `+` SHR REUSE /* TABLES */ ALLO`CATE FIL`E`(ISPLLIB) +` DATASET('SYS1`.MIGLIB') +` SHR REUSE /* CODE */ ALLO`CATE FIL`E`(SYSHELP) +` DATASET('SYS1`.HELP') +` SHR REUSE /* HELP TEXT */ IPCS `NOP`A`RM` /* I`PC`S `LIN`E `MOD`E */ ISPS`TART PGM`(`BLSG) NEW`A`PPL(BLSG) +` PASSLIB PARM(`PANEL(B`L`SPPRIM)) /* IPCS DIA`LO`G` `*`/ Figure 2-2. Sample CLIST to Add IPCS Libraries to Data Set Concatenations and Access IPCS.

See IPCS Customization for more information about customizing access to the IPCS dialog.

---

[Previous](2-5.md) | [Index](README.md) | [Next](2-7.md)
