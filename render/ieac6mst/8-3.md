[Previous](8-2.md) | [Index](README.md) | [Next](8-4.md)

---

## 8.3 Copying and Clearing Dumps in Batch Mode

<a id="HDRCPY1"></a>

be used to copy a dump from one tape to another. [Figure 8-3](8-3.md) shows how to use the IPCS COPYDUMP subcommand to copy a dump. Note that this job specifies IPCS NOPARM, to avoid accessing SYS1.PARMLIB.

<a id="BATCP"></a>

> `//IP`CS`JOB` `JOB 'acctinfo','I`P`CSU1 output',MSGLEVEL=(`1,1), // MSGC`LASS=A,CLASS=J,NOTIFY=IPCSU1` //IP`CS` EXEC `PGM`=`IKJEFT01,REGION=600K` /* `//IP`C`SD`D`IR DD DSN=IPCSU1.DUMP.DIR,DISP`=(SHR) A `//T`APE`IN` `DD UNIT=TAPE,LABEL=(1,NL),VOL=SER=D`MPIN, addr`es`s `// DCB=(DSORG=PS,RECFM=FBS,BLKSIZE=29120,`LRECL=4160) addr`ess` `//`T`APEOUT DD UNIT=TAPE,LABEL=(2,NL),VOL=`SER=DMPOUT, addr`es`s `// DCB=(DSORG=PS,RECFM=FBS,BLKSIZE=29120,`LRECL=4160) //SY`STSPRT DD` S`YS`O`UT=*` //SY`STSIN DD` IPCS `NOP`A`RM` COPY`DUMP INF`I`LE(TAPEIN) OUT`F`ILE(TAPEOUT) NOP`R`INT NOC`O`NFIRM` END /* Figure 8-3. Running the COPYDUMP Subcommand in Batch Mode.

[Figure 8-4](8-3.md) shows how to copy and clear a SYS1.DUMPxx data set.

<a id="CPYCLER"></a>

> `//IP`CS`JOB` `JOB 'acctinfo','I`P`CSU1 output',MSGLEVEL=(`1,1), // MSGC`LASS=A,CLASS=J,NOTIFY=IPCSU1` //IP`CS` EXEC `PGM`=`IKJEFT01,REGION=600K` > `//IPC`S`DD`I`R DD DSN=IPCSU1.DUMP.DIR,DISP=(OLD,`KEEP) . `//D`UMP`IN` `DD DSN=SYS1.DUMP99,DIS`P=SHR /* `//D`UM`PO`U`T DD DSN=SYS1.OFFDMP99,DISP=(OLD`,KEEP) //SY`STSPRT DD` S`YS`O`UT=*` //SY`STSIN DD` IPCS `NOP`A`RM` COPY`DUMP INF`I`LE(DUMPIN) OUT`F`ILE(DUMPOUT) CLE`A`R NOP`R`INT NOC`O`NFIRM` END /* Figure 8-4. Copying and Clearing a SYS1.DUMPxx Data Set in Batch Mode.

See [Appendix B, "Examples of JCL to Print, Copy, and Clear a Dump Data](b-0.md) [Set" in topic B.0](b-0.md) for more examples of JCL to manage SYS1.DUMPxx data sets.

---

[Previous](8-2.md) | [Index](README.md) | [Next](8-4.md)
