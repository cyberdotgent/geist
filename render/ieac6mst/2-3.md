[Previous](2-2.md) | [Index](README.md) | [Next](2-4.md)

---

## 2\.3 Specifying Parameters on the IPCS Command

<a id="HDRSPECP"></a>

When you enter the IPCS command, you specify parameters for the IPCS session\. These parameters control the defaults for the IPCS print file and activate the IPCS problem management and data set management subcommands\. If you have the IBM Information/Family programs for problem management, you do not need the problem and data set management feature of IPCS\.

**Note:** These parameters do not control the session defaults specified by the SETDEF subcommand\. See ["Setting Session Defaults" in topic 2\.7](2-7.md) for more information\.

The IPCSPRxx parmlib member contains the session parameters\. The PR\(nn\) keyword on the IPCS command controls which set of parameters to use for your IPCS session\. You have the following choices when starting an IPCS session:

- If you do not want to use the IPCS problem and data set management subcommands, enter:

`IPCS NOPARM`

NOPARM indicates that the IPCS session is for problem analysis only\. IPCS does not use any IPCSPRxx parmlib member\. NOPARM improves performance when IPCS initializes a dump or trace for processing\. However, the problem management and data set management subcommands will not be available\.

NOPARM only affects the IPCSPR00 member of SYS1\.PARMLIB\. IPCS still uses BLSCECT and all its imbedded members of SYS1\.PARMLIB during your IPCS session\.

- If you want to specify defaults for the IPCS print file, but not use the problem and data set management subcommands, enter:

`IPCS`

IPCS starts the session using the parameters in the default parmlib member IPCSPR00 that is shipped with IPCS\. If that member is not usable, IPCS diagnoses the errors detected and ends processing of the IPCS command\.

- To start the session using an alternate set of parameters located in the parmlib member, IPCSPR*nn*, enter:

`IPCS PR(nn)`

*nn* is a one or two digit decimal number, which IPCS appends to 'IPCSPR', forming the name of a PARMLIB member\. That member, IPCSPR*nn*, contains the parameters for this session\. You usually do not need to specify parameters unless you will use IPCS problem management or the data set management subcommands\.

---

[Previous](2-2.md) | [Index](README.md) | [Next](2-4.md)
