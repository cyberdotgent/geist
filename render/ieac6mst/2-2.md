[Previous](2-1.md) | [Index](README.md) | [Next](2-3.md)

---

## 2.2 Alternative Methods for Accessing IPCS

<a id="HDRALTER"></a>

```text
   There are many ways to access IPCS.  Each method depends upon you or your
   installation's desire to change the default allocations and customize the
```

access to IPCS. c.cp 999 | IPCS has several files which you can manually allocate to customize your session. [Figure 2-1](2-2.md) summarizes these files:

<a id="IPCSFIL"></a>

<a id="IPCSFIL"></a>

| Figure 2-1. Default Allocations of IPCS Files

| Data Set | File | Default | More Information |
| --- | --- | --- | --- |
| Dump directory | IPCSDDIR | userid.DDIR | "Preparing a Dump<br>Directory" in<br>topic 5.1 |
| Parmlib data set | IPCSPARM | SYS1.PARMLIB | "Providing Format<br>and Analysis<br>Support" in<br>topic 1.2.4 |
| Print data set | IPCSPRNT | none | "Print and Table of<br>Contents Files" in<br>topic 9.1 |
| Table of contents | IPCSTOC | none | "Print and Table of |
| data set |  |  | Contents Files" in<br>topic 9.1 |


The following procedure gives an alternative method for accessing IPCS. | This procedure shows the proper sequence for allocating the optional IPCS | files and gives an example of how to perform each step using a TSO/E command. ["Using a TSO/E Logon Procedure to Access IPCS" in topic 2.5](2-5.md) and ["Using a CLIST to Access IPCS" in topic 2.6](2-6.md) give other examples of how to | access IPCS. Remember that the procedure you should follow to access IPCS | depends on the extent that you or your installation has customized the | procedure. 1. **Log**o**n** t**o TSO/**E.

2. **Concatenate** **IPCS** **libraries**. This step can be done anytime before step

[10](2-2.md). . `ALL`O`CATE FILE(SYS`P`ROC) DSN('SYS1.SBLSC`L`I0'`) SHR . `ALL`O`CATE FILE(ISP`T`LIB) DSN('SYS1.SBLST`B`L0'`) SHR . `ALL`O`CATE FILE(ISP`S`LIB) DSN('SYS1.SBLSK`E`L0'`) SHR . `ALL`O`CATE FILE(ISP`P`LIB) DSN('SYS1.SBLSP`N`L0'`) SHR . `ALL`O`CATE FILE(ISP`M`LIB) DSN('SYS1.SBLSM`S`G0'`) SHR

**Note:** If you receive a "FILE IN USE" message on the allocate, you will need to either free that file using the FREE command or specify the REUSE option on the ALLOCATE command.

<a id="LIScvsam"></a>

3. **Create** **your** **dump** **directory** **VSAM** **data** **set**, using the TSO/E DEFINE

command. If you already have a dump directory, skip to step [5](2-2.md) `DEFI`N`E CLUSTER(NAME('userid.DDIR`'`) VOLUMES(VSAMn`n`)` / `INDEX(NAME('userid.DDI`R`.I') TRA`C`KS`(`1` 1) === | `DATA(NAME('userid.D`D`IR.D') CYLI`N`DE`R`S(1 1) K`E`YS`(128 0) `RECORDSIZE(3`8`4 307`2`)` See Cha [pter 5, "Using the IPCS Dump Directory" in topic 5.0 fo](5-0.md) r more | information.

<a id="LISinitdir"></a>

4. **Initialize** **the** **dump** **directory** **data** **set** you just created using the TSO/E IPCSDDIR command:

`IPCSDD`I`R 'userid.DDI`R' See Cha [pter 5, "Using the IPCS Dump Directory" in topic 5.0 fo](5-0.md) r more | information. c.cp 999

<a id="LISalcdir"></a>

5. **Allocate** **the** **dump** **directory** **data** **set** by entering the following TSO/E command:

`ALLOCA`T`E DDNAME(IPCSDDI`R`) DATASET('userid.DDIR`'`) S`HR See Cha [pter 5, "Using the IPCS Dump Directory" in topic 5.0 fo](5-0.md) r more | information.

<a id="LISalcprt"></a>

6. **Allocate** **a** **print** **file** if you plan on printing any IPCS output. You can assign a data set or a SYSOUT class:

`ALLOCATE` `DDNAME(IPCSPRNT)` `SYSOUT(A)`

See "Pr [int and Table of Contents Files" in topic 9.1 fo](9-1.md) r instructions | on creating and using the print file.

<a id="LISal"></a>

7. **Allocate** **a** **table** **of** **contents** **file** if you plan on printing any IPCS output and want a table of contents of the output. You can assign a data set or a SYSOUT class:

`ALLOCATE` `DDNAME(IPCSTOC)` `SYSOUT(A)`

See "Pr [int and Table of Contents Files" in topic 9.1 fo](9-1.md) r instructions | on creating and using the table of contents file. | 8. **Alloca**t**e** **a parml**i**b da**t**a s**et if you want to use something other than | SYS1.PARMLIB. _ `ALL`O`CATE DDNAME(IPCS`P`ARM) DSN('DEPTXYZ.PAR`M`LIB' 'SYS1.PARM`L`IB'`) SHR See "Pr [oviding Format and Analysis Support" in topic 1.2.4 fo](1-2-4.md) r more | information.

9. **Start** **an** **IPCS** **line** **mode** **session**, by entering the TSO/E IPCS command.

`IP`CS See "Sp [ecifying Parameters on the IPCS Command" in topic 2.3 fo](2-3.md) r | information on specifying parameters on the IPCS command.

<a id="LISstdia"></a>

10. **Start** **the** **IPCS** **dialog.** You can either go into ISPF to invoke the IPCS dialog or enter the following TSO/E command:

/* `IS`P`START PGM`(`BLSG) NEWAPPL`(`BLSG) P`A`SSLIB PARM(PANEL(BLSP`PRIM)) See "St [arting the IPCS Dialog" in topic 2.4 fo](2-4.md) r more information.

---

[Previous](2-1.md) | [Index](README.md) | [Next](2-3.md)
