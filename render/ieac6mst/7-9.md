[Previous](7-8-5.md) | [Index](README.md) | [Next](7-10.md)

---

## 7.9 Writing IPCS CLISTs

When writing CLISTs that contain TSO/E commands, you must consider the | environment in which the CLIST will run when designing the CLIST. This is | especially important when designing CLISTs that contain both TSO/E | commands and IPCS subcommands. | When you invoke a CLIST from the IPCS dialog, the CLIST can use IPCS | subcommands to place information in ISPF function pool dialog variables | and can use the ISPEXEC subcommand to request dialog services that use the

dialog variables. If TSO/E Release 2 is not installed, ISPF has no | knowledge of any CLIST variables used in a CLIST initiated through this | panel. Therefore, such CLISTs cannot reference ISPF variables. | Given this flexibility, the functions that can be used in a CLIST are | dependent on the environment from which you invoke the CLIST. Before you | write the CLIST, decide what environment you will typically invoke the

CLIST from. Then review [Figure 7-13](7-9.md#CLISTS) for restrictions. c.cp 999

<a id="CLISTS"></a>

<a id="CLISTS"></a>

SI SYSOUTTRAP command, used in IPCS CLISTs

| Figure 7-13. Considerations for Designing and Invoking IPCS CLISTs | CLIST Contains: | Invoke as Follows: |
| --- | --- | --- |
| IPCS subcommands | In IPCS line mode, batch mode, or through the IPCS | dialog |
| TSO/E commands | In IPCS line mode, batch mode, or through the IPCS<br>1. Invoke the CLIST with the TSO subcommand if<br>authorized commands and functions such as | \|<br>The CLIST contains a combination of TSO/E<br>\| |
| \| | the same names as IPCS subcommandsSRELISone | while in the IPCS dialog<br>One or more TSO/E commands in the CLIST have<br>the items listed in step 1 are not true. |
| A combination of | From IPCS line or batch mode, or through the IPCS | \| |
| dialog, there are three possible ways of invoking | \| | and TSO/E |
| \| | commands<br>the IPCS subcommands in the CLIST must be invoked | \| |
| by using the BLSGSCMD dialog program. | subcommand. By invoking the CLIST in this way, | 2. Invoke the CLIST without using the TSO<br>\| |
| you do not have to use the BLSGSCMD dialog | \| |  |
| \| | following:<br>TSO/E commands that have the same names as<br>IPCS subcommands. | CLIST. You can only invoke the CLIST in this way<br>if the CLIST does not contain either of the<br>\|<br>\| |
| A combination of TSO/E authorized commands | \| |  |
| \| | 3. Invoke the CLIST without using the TSO subcommand | applies only when you invoke the CLIST from<br>\| |
| to invoke the CLIST, but instead using the TSO | \| |  |
| \| |  | commands. By using this method, you do not have<br>to use the BLSGSCMD dialog program to invoke the<br>IPCS subcommands. |
| ISPF commands | Invoke the CLIST from the IPCS dialog or through | \| |
| batch mode IPCS if ISPF is active in batch. Do not | \| |  |
| \| |  | the IPCS dialog. The TSO subcommand prevents the |


See IPCS Customization for the BLSGSCMD dialog program.

---

[Previous](7-8-5.md) | [Index](README.md) | [Next](7-10.md)
