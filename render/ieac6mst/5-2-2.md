[Previous](5-2-1.md) | [Index](README.md) | [Next](5-3.md)

---

### 5.2.2 Dump Directories and Performance

| IPCS relies on a dump directory for efficient processing of dumps.  After         | a period of use, however, the dump descriptions in these VSAM data sets ?  | become fragmented.  cfont 3 8 L 12 3 L 16 7 L 24 8 L 33 4 L         | Changing the Control Interval Size:  Increasing the control interval size      | of the dump directory helps decrease the number of splits during dump

Changing the Control Interval Size: Increasing the control interval size of the dump directory helps decrease the number of splits during dump

processing. See Access Method Services Reference for information about

control interval splits and IPCS Customization for information about changing the IPCS-supplied default in BLSCDDIR. Refreshing a Dump Directory: [Figure 5-1](5-2-2.md) is a sample CLIST to reorganize a | dump directory. It copies the dump descriptions into a temporary data | set, then copies them back into the dump directory, allowing VSAM to | rearrange the data.

<a id="REPDIR"></a>

| `CONTROL` `LIST` === `| A`L`LOCATE FILE(SA`V`EDDIR) UNIT(`S`YSV`I`O) CYL S`P`ACE`(`10 10) R`E`C`FM(U) +

`BLKSIZE(3072)` `REUSE` `NEW`

`REPRO` `INDATASET(&DSN)` `OUTFILE(SAVEDDIR)`

. R`EPRO INFILE(SAVE`D`DIR) OUTDATASET(`&`DSN)` REUSE

`FREE` `FILE(SAVEDDIR)` Figure 5-1. Sample CLIST to Refresh a Dump Directory.

```text
    ________________________________________________________________________ 
   |                                                                                  |
   |                                                                                  |
    __________________________________________ 
 | |   PROC 1 DSN                                                                     |
```

Using a Central Dump Directory for Initialization: When IPCS initializes a dump, it writes a large amount of information about a dump to a dump

| di`rect`o`r`y`.` Use a central dump directory for the initial dump analysis, | then copy the description into your dump directory using COPYDDIR. | COPYDDIR allows VSAM to rearrange the dump description as it is copied to | your dump directory.

---

[Previous](5-2-1.md) | [Index](README.md) | [Next](5-3.md)
