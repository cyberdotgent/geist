### 14.10.1 Setting Up a Data/Text Merge Application

| source. | The column list data/text merge is a faster merge than multiple letter | merge. All the data is retrieved at one time and placed in the document. | Then one copy of the document is created during the print or MRGDOC

| command. | A direct merge is a fast merge. Use it only for documents that are to be

| printed immediately or for data that does not change.
