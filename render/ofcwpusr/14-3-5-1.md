#### 14.3.5.1 Describing Data in Files Using IDDU

these records, you must use Query.

The steps required to define files are explained in the IDDU User's Guide. Data must be defined before it can be merged into a document. The steps

required to describe data fields in a file are:

1. Create a data dictionary.

2. Create data definitions.

Create field definitions.

Create a format definition.

Create a file definition or link the IDDU definition to an

existing file.

3. Create a file.

4. Type information into a file.
