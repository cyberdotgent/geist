[Previous](1-1-18.md) | [Index](README.md) | [Next](1-1-20.md)

---

## 1\.1\.19 Diagnose Code X'44'\-\-Voluntary Time Slice End

**Privilege Class:** Any

Use DIAGNOSE code X'44' to notify the VM/ESA scheduler that a spin lock loop exists in your virtual machine\. DIAGNOSE code X'44' informs the VM/ESA scheduler that the remainder of the CPU time slice allocated to a virtual CPU is no longer useful\. VM/ESA and MVS/SP 2\.1\.1, as well as subsequent releases, use DIAGNOSE code X'44' as the standard method for notifying the VM/ESA scheduler that a spin lock loop exists\.

**Entry Values:** Specify the Rx and Ry fields as zero\.

**Usage Notes:**

1\. The virtual CPU that issues DIAGNOSE code X'44' is given a dispatching priority lower than other virtual CPUs in the same guest multiprocessing configuration\. The other virtual CPUs generally run before the issuing virtual CPU\. If there is no other virtual CPU for this virtual machine, DIAGNOSE code X'44' is not operational\.

2\. DIAGNOSE code X'44' is useful when the operating system on one guest CPU is waiting to obtain a spin lock, and another guest CPU must be run in order to release the spin lock\. The DIAGNOSE causes the CPU holding the spin lock to be run before the issuing CPU runs again\.

3\. The effects of DIAGNOSE code X'44' are temporary\. Within a few seconds, the virtual CPU is scheduled as if the DIAGNOSE were never issued\.

**Responses:**

None\.

---

[Previous](1-1-18.md) | [Index](README.md) | [Next](1-1-20.md)
