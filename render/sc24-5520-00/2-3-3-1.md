[Previous](2-3-3.md) | [Index](README.md) | [Next](2-3-3-2.md)

---

#### 2.3.3.1 IUCV Messages

To use IUCV, you and the virtual machine users with whom you use IUCV to communicate must code application programs that invoke the IUCV macro. By specifying one of eighteen IUCV functions (one of which is the SEND function) on the IUCV macro, you tell IUCV what to do.

As mentioned at the beginning of the chapter, IUCV is a facility that enables a virtual machine to communicate with other virtual machines, CP system services, and with itself. An IUCV communication is called a **message**. An IUCV message is created when an application program running in a virtual machine specifies the SEND function on the IUCV macro. The virtual machine that invokes the SEND function is called the **source** virtual machine. The message travels to the **target** virtual machine over a [FIGC92FIGZ](2-3-3-1.md) predefined linkage called a **path**. Figure 8 illustrates how a simple IUCV communication takes place.

<a id="C92FIGZ"></a>

Sourc`e Virt`u`al Mach`i`ne` Targe`t Virt`u`al Mach`i`ne` The s`our`c`e virt`u`al mach`i`ne` invok`es the` I`UCV` `macr`o`,` creat`ing a me`s`s`a`ge that` IUCV `send`s `over` `a pa`t`h`. `---`PA`TH---> to` th`e` t`arg`e`t virt`u`al mach`i`ne.` Figure 8. IUCV Communications

---

[Previous](2-3-3.md) | [Index](README.md) | [Next](2-3-3-2.md)
