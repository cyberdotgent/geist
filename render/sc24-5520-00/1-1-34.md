[Previous](1-1-33.md) | [Index](README.md) | [Next](1-1-35.md)

---

## 1.1.34 Diagnose Code X'94'--VMDUMP and Symptom Record Service

<a id="HDRDC94"></a>

**Privilege** **Class:** Any Use DIAGNOSE code X'94' to do one or more of the following functions: Request a dump of guest real storage to a system data file, in VMDUMP format, that can be processed by the dump viewing facility Request that CP process a symptom record. A copy of the completed symptom record is placed in the dump file (if a dump was requested), and copies of the completed symptom record are available to authorized symptom record recording virtual machines. Symptom records should be used to record suspected errors in IBM software or in IBM hardware. **Entry** **Values:** Rx Is the address of the parameter string in guest real storage. Ry Is the length of the parameter string. When a request to process a symptom record accompanies a dump request, Ry should not be register 15. **Supported** **Parameters** The parameter string must be no longer than 240 bytes, and must not be fetch-protected from the invoking program. It is a character string with the same format as the operands of the VMDUMP command, with the exception of the parameters described below, which are unique to DIAGNOSE code X'94'.

<a id="FIGUNIQ26 { } { DUMP list addr } { } { } { } { } { hexloc1 { } hexloc2 }"></a>

{ 0 {:} END } { ALL } { } TO { ... } To userid { } SYSTEM { {.} length } { END } { ALL } { } { } " [NORETURN] FORMAT {vmtype} DCSS [XA] [*dumpid] {FILE } DSS [SR symptom record addr [NODUMP]] CLOSE CONt CANCEL NOCont

DUMP list-addr indicates that the dump address ranges are in hexadecimal form within a separate parameter list. list*-addr is* the address of the dump address list in guest real storage. It is expressed as a 4-byte hexadecimal address immediately following the DUMP keyword and a single blank. See "Dump [Address List" for](1-1-34.md) a complete description of the dump address list. NORETURN indicates that the invoking virtual machine may not transfer the spool file back to itself from the dump receiver, using the TRANSFER or CHANGE command. This parameter must be used in conjunction with the SYSTEM or TO user*id (in* other words, not your own user ID) parameter designating the authorized receiver of the dump file. This facility can be used to restrict access to dumps of shared virtual virtual machine storage to certain authorized users.

SR symptom-record-addr indicates a symptom record is to be processed by CP. symp*tom-record-addr is* the address of the symptom record in guest real storage. It is expressed as a 4-byte hexadecimal address immediately following the

SR keyword and a single blank. This parameter may be used to record suspected software errors in IBM products. Symptom records should not be used to record end-user errors. Errors that are presented through DIAGNOSE X'94' using the

SR keyword are those that need the attention of the personnel who are responsible for first-level problem determination at an installation. NODUMP indicates that the symptom record is not to be included in a VMDUMP file, but just made available to authorized symptom record recording virtual machines. This parameter may only be used in conjunction with the

SR parameter indicating the address of a symptom record to be processed. The remaining parameters are exactly as specified for the VMDUMP command in VM/ESA CP Command and Utility Reference. Usage Notes Regarding Dumping a Virtual Machine 1. The requested dump file contains only storage that the user is authorized by storage key to access. Any block of storage that is fetch-protected with a key other than that of the invoking program is not dumped. A virtual machine program that executes in key zero can dump any part of guest real storage. Shared segments which comprise named saved systems and discontiguous saved segments are also dumped, subject to the same protection rule. Only second-level storage (that is, guest real storage) is dumped. Operating systems that execute in translate mode create third-level (guest virtual) storage. 2. The DUMP parameter with a separate dump address list and the inline address range(s) provided as on the VMDUMP command may not be specified on the same invocation of DIAGNOSE X'94' 3. The absence of discontiguous saved segments when a DCSS is specified results in an error only if no other area of storage was requested. 4. The dump address list must contain at least one address range. 5. The DUMP and NORETURN parameters may not be specified on the same invocation of DIAGNOSE X'94' with CLOSE, CANCEL, CONT, or NOCONT. See VMDUMP command documentation for a list of other parameters with the same restriction. 6. When multiple DIAGNOSE code X'94' invocations are used in continuous output mode, the NORETURN parameter is only accepted on the first request that opens the file. Once successfully specified, it may be repeated on later requests. However, if it is not specified on the first request, subsequent requests (affecting the same VMDUMP file) that specify NORETURN cause an error. 7. When multiple DIAGNOSE code X'94' invocations are used in continuous output mode, the DUMP parameter may be specified on any request to dump storage. 8. See also the VMDUMP command usage notes in *VM/ESA* *CP* *Command* *and* Util*ity Ref*e*rence.*

<a id="HDRDAL"></a>

Dump Address List: Your virtual machine program that issues DIAGNOSE X'94' may provide one or more dump address lists containing address ranges to be dumped. These lists are in hexadecimal format. If more than one is provided, they are linked by a pointer in the first word of each. You must store the fullword address of the first (or only) list into the DIAGNOSE X'94' parameter list described above, following the "DUMP" keyword and separated from it by a blank. The format of each dump address list is the following: 00 NEXT LIST ADDRESS ADDRESS RANGE COUNT 08 TYPE //////////////////////////////////////////////////////// 10 ADDRESS RANGE DATA NEXT-LIST-ADDRESS is the fullword address of the next dump address list or a fullword of zero if no further list exists. ADDRESS-RANGE-COUNT is the fullword count of the number of address ranges included in this dump address list. It must be greater than zero. TYPE Is a 1-byte flag indicating whether the address ranges in this list are specified as address/length (X'00') or address/address (X'80'). 1... .`...` (`X'80`'`)` `0` `=` ad`d`r`e`s`s/length` 1 = address/address .111 1`1``11` (`X'7F`'`)` `re``s``e`rv`ed` ADDRESS-RANGE-DATA is pairs of fullwords containing the starting address of each range followed by either the length or the ending address, depending on the setting of the range-type flag. The number of 8-byte range entries is contained in the range-count field. Usage Note Regarding Dump Address Lists The dump address list must contain at least one address range. Usage Notes Regarding Symptom Records 1. A symptom record is processed only if it resides within storage that the user is authorized by storage key to access. If the storage within which the symptom record resides is fetch-protected with a key other than that of the invoking program, the symptom record is not processed; if a dump was requested along with symptom record processing, CP generates a symptom record to be placed in the virtual machine dump. This record contains only CP environment information. A symptom record supplied by a virtual machine that executes in key zero is always processed, regardless of where in guest real storage the symptom record resides. 2. The virtual machine that issues the DIAGNOSE X'94' must initialize the required fields within the symptom record with information describing a problem before it issues DIAGNOSE X'94'., c.cp 3. CP updates the symptom record to contain the following information: CP environment data in section 1 The architectural level of the CP symptom record processor (C'10') in section 2 Return and reason codes in section 2.1. If the invoking program is running with a PSW key that is neither the storage key of the symptom record nor zero, the system records the symptom record but does not update the invoker's copy in virtual machine storage with CP environment data. 4. Symptom records which are over 3500 decimal bytes in length are truncated before being placed in a dump or made available to symptom record recording virtual machines. 5. All fields designated by an RS are updated within the caller's symptom record by CP. The conventions for initializing the contents of the RS (required from system service) fields are: a. The customer-assigned system node name (ADSRID) is the value specified on the SYSID macro (which is a macro within HCPSYS) for the CPU on which CP is running while processing the DIAGNOSE X'94' request. b. The ADSRTOD field is in Greenwich Mean Time. The ADSRGMT field contains the clock zone differential that may be of value to users who want to convert to local. If the STCK instruction completes with a non-zero condition code, the clock value (ADSRTIME) is hex zeros, and the printable time/date fields are blanks. 6. When the

SR parameter is coded, registers Rx+1 and Ry+1 reflect the results of symptom record processing in the form of hexadecimal return codes and reason codes. If the symptom record storage within the virtual machine can be written to, the ADSRRET and ADSRREA fields in section 2.1 contain these same results. The return code and reason code value reflect the most severe level of error that occurred. The reason code value contains the value for the first error found at the severity level indicated by the return code. If the symptom strings in sections 3 and 4 contain other than alphamerics, national, pound sign (#), slashes (/) and blanks, a return code of 4 and a reason code of X'0E08' are generated. 7. The SDB syntax of the symptoms in sections 3 and 4 is not validated. 8. If a DIAGNOSE X'94' is issued with the

SR keyword in the parameter list, and section 1 of the symptom record is already initialized, the CP environment sections (RS fields) are not updated within the symptom record. An unmodified copy of the symptom record is sent to each virtual machine connected to the CP *SYMPTOM system service. 9. The

SR and NODUMP parameters may not be specified on the same invocation of DIAGNOSE X'94' with CLOSE, CANCEL, CONT, or NOCONT. 10. When multiple DIAGNOSE X'94' invocations are used in continuous output mode, the

SR parameter can be specified on any one invocation to supply the symptom record in the dump. When a program issues a DIAGNOSE X'94' request only to add a symptom record and does not want to dump any areas of guest storage, it should include a dummy address range of zero. This is required because, if no address range or DUMP address list is specified, all of guest storage is dumped. Since page zero is always included in VMDUMP files, a request such as "0

SR addr" does not result in any added output. 11. Input symptom record storage Unused space is not compressed from the symptom record. The symptom record sections are written in the sequence and at the displacements in which they are presented. No unused symptom record storage should be passed by means of the DIAGNOSE X'94' instruction. For example, there should not be preassigned, unused space at the end of symptom record sections 3, 4, or 5. The length for that section (contained in section 2) should only be the length of the util**ized por**tion of the section when the DIAGNOSE X'94' is issued. Otherwise, the common storage that is allocated to the service routine and the CMS symptom record file contain wasted space. 12. Symptom record recording considerations When a DIAGNOSE X'94' is issued with a parameter list containing an

SR keyword, CP sends copies of the symptom record asynchronously to authorized virtual machines that are connected to the CP *SYMPTOM System Service. Control is returned to the virtual machine that issued the DIAGNOSE X'94' instruction after a copy of the symptom record is placed on the VM/ESA recording queue. **Responses:** Upon completion of DIAGNOSE code X'94', control is returned to the invoker with a condition code set to indicate the status of both input parameter list processing and dump processing. A return code in Ry indicates the results of processing the input parameter string and the dump request (if present). If symptom record processing was requested, return and reason codes in registers Ry+1 and Rx+1, respectively, indicate the result of symptom record processing (condition codes are not used to reflect the status of symptom record processing). The condition codes are listed in [Table 13](#TBL94CCOD).

<a id="94CCOD"></a>

Table 13. DIAGNOSE Code X'94'--Condition Codes

| Condition | Code Description |
| --- | --- |


The return codes in Ry are listed in [Table 14](#TBL94RET1).

<a id="94RET1"></a>

Table 14. DIAGNOSE Code X'94'--Return Codes

| Code | Code | Description |
| --- | --- | --- |
| X'00' | 0,1 | Successful completion:<br>CC=0 indicates that all requested areas were<br>ranges or dump address list, and no DCSSs are<br>loaded. |
| X'04' | 2 | The parameter list exceeds 240 bytes. |
| X'08' | 1,2 | System I/O error reading a guest page.<br>CC=1 indicates at least one requested page could<br>not be dumped.<br>CC=2 indicates the parameter listSR buffer, or<br>dump address list could not be accessed. |
| X'0C' | 1,2 | Storage access violates guest fetch protection.<br>CC=1 indicates at least one requested page could<br>not be dumped.<br>CC=2 indicates the parameter list or dump address<br>address less than start, a range length of 0<br>given, or too many ranges in the address list. (A<br>single list must fit within a page of storage.) |
| X'14' | 2 | Conflicting options |
| X'18' | 2 | User ID missing or longer than eight characters |
| X'1C' | 2 | Hexloc missing or invalid hexadecimal value. If a<br>dump address list is used, the number of ranges<br>specified was zero. |
| X'20' | 2 | Required parameter missing or invalid |
| X'2C' | 2 | Hexloc exceeds storage size |
| X'30' | 2 | Return code unused but reserved for compatibility<br>with VM/ESA (370 Feature) |
| X'34' | 2 | An invalid address points to storage outside of<br>user area. |
| X'38' | 2 | A soft abend occurred during processing. |
| X'3C' | 1,2 | Unaddressable range given. At least one range<br>given was above guest storage and did not fall<br>within a discontiguous saved segment. The<br>unaddressable ranges are not dumped.<br>CC=1 indicates the valid addresses given are |


SR parameter was specified, the return codes shown in [Table 15](#TBL94RET2) are placed in Ry+1:

<a id="94RET2"></a>

Table 15. DIAGNOSE X'94' Symptom Record Processing Return Codes

| Return | Code Description<br>entire input symptom record was rec<br>partial symptom record was recorded.<br>None of the symptom record was reco<br>None of the symptom record was reco |
| --- | --- |


The reason code shown in [Table 16](#TBL94RET3) may be returned in Rx+1 when Ry+1 contains a return code value of X'00':

<a id="94RET3"></a>

Table 16. DIAGNOSE X'94' Reason Codes for Return Code X'00'.

| Reason | Code Description |
| --- | --- |


The reason codes shown in [Table 17](#TBL94RET) may be returned in Rx+1 when Ry+1 contains a return code value of X'04':

<a id="94RET"></a>

[Table: Table 17. DIAGNOSE X'94' Reason Codes for Return Code X'04'.]

Reason

The reason codes shown in [Table 18](#TBL94RET5) may be returned in Rx+1 when Ry+1 contains a return code value of X'08':

<a id="94RET5"></a>

Table 18. DIAGNOSE X'94' Reason Codes for Return Code X'08'.

| Reason | Code Description |
| --- | --- |


The reason codes shown in [Table 19](#TBL94RET4) may be returned in Rx+1 when Ry+1 contains a return code value of X'12':

<a id="94RET4"></a>

Table 19. DIAGNOSE X'94' Reason Codes for Return Code X'12'.

| Reason | Code Description<br>the characters "SR."<br>entries of section 2.1. |
| --- | --- |

---

[Previous](1-1-33.md) | [Index](README.md) | [Next](1-1-35.md)
