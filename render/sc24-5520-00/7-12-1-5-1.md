[Previous](7-12-1-5.md) | [Index](README.md) | [Next](7-12-1-5-2.md)

---

## 7.12.1.5.1 Parameter Lists Passed for Commands

<a id="HDRPARPC"></a>

#### Parameter Lists Passed for Commands For the AUTOLOG and XAUTOLOG commands, the following parameter list is passed:

BYTE OFFSET LA`BEL` FI`ELD` DE`C(HEX)` BY`TES` CO`NTENTS` --`-------------------------------------------------------------------` AC`IFCN` Fu`nction` 0(`00)` 1 AC`ILOG` Request code AC`ICODE` Re`turn c`o`de` 1(`01)` 1 AC`I s`e`ts t`h`is f`i`eld t`o indicate completion of the request.* AC`ILEN` Le`ngth o`f 2(`02)` 2 Th`is f`i`eld i`s `s`e`t t`o `t`h`e` ACIPARMS total length `of AC`I`PARMS` i`n` bytes.( AC`IBMAPA` AC`I a`u`dit s`e`t-` 4(`04)` 1 AC`I b`i`t s`e`ttings a`r`e o`b`tain-` tings. Bits ed from the c`om`m`and` e`ntr`y 0-7 contain t`he` for the comma`nd.` audit setting for versions `A-H`, respectively. AC`IBMAPP` AC`I p`r`otect` 5(`05)` 1 AC`I b`i`t s`e`ttings a`r`e o`b`tain-` settings. Bit`s` ed from the c`om`m`and` e`ntr`y 0-7 contain t`he` for the comma`nd.` protect setti`ngs` for versions `A-H`, respectively. AC`IENV` Ty`pe o`f `e`v`ent` 6(`06)` 1 AC`IXAC` AC`IRGRP` Re`questor` 8(`08)` 8 Bl`anks` group name* AC`IRUSR` Re`questor` 16`(10)` 8 Th`e u`s`er I`D `o`f `t`h`e u`s`er w`h`o` user ID is being auto`ma`t`icall`y `logged` on AC`IFLAG` Fl`ag b`y`te` 68`(44)` 1 AC`IGOOD` AC`ICMDTP` Co`mmand v`e`r-` 69`(45)` 1 X'`C0'` sion that is being execute`d` AC`IEVENT` 72`(48)` AU`TOLOG o`r `X`A`UTOLOG` AC`IDATA` Ad`ditional` 12`0(78)` 8 Co`mmand l`i`ne` data The command processor initializes ACIPARMS to pass the following information to HCPRPW. For the LOGON command, the following parameter list is passed: BYTE OFFSET LA`BEL` FI`ELD` DE`C(HEX)` BY`TES` CO`NTENTS` --`-------------------------------------------------------------------` AC`IFCN` Fu`nction` 0(`00)` 1 AC`ILOG` request code AC`ICODE` Re`turn c`o`de` 1(`01)` 1 AC`I s`e`ts t`h`is f`i`eld t`o indicate completion of the request.* AC`ILEN` Le`ngth o`f 2(`02)` 2 Th`is f`i`eld i`s `s`e`t t`o `t`h`e` ACIPARMS total length `of AC`I`PARMS` i`n` bytes.( AC`IBMAPA` AC`I a`u`dit s`e`t-` 4(`04)` 1 AC`I b`i`t s`e`ttings a`r`e o`b`tain-` tings. Bits ed from the c`om`m`and` e`ntr`y 0-7 contain t`he` for the comma`nd.` audit setting for versions `A-H`, respectively. AC`IBMAPP` AC`I p`r`otect` 5(`05)` 1 AC`I b`i`t s`e`ttings a`r`e o`b`tain-` settings. Bit`s` ed from the c`om`m`and` e`ntr`y 0-7 contain t`he` for the comma`nd.` protect setti`ngs` for versions `A-H`, respectively. AC`IENV` Ty`pe o`f `E`v`ent` 6(`06)` 1 AC`IXAC` AC`IRGRP` Re`questor` 8(`08)` 8 Bl`anks` group name* AC`IRUSR` Re`questor` 16`(10)` 8 Th`e u`s`er I`D `o`f `t`h`e u`s`er w`h`o` user ID is being logg`ed` `on(` AC`IFLAG` Fl`ag b`y`te` 68`(44)` 1 AC`INOREC -` `o`n `i`f `t`h`is i`s not a reconnect ACIANY - unconditionally on AC`ICMDTP` Co`mmand v`e`r-` 69`(45)` 1 X'`C0'` being execute`d` AC`IEVENT` 72`(48)` LO`GON` AC`IDATA` Ad`ditional` 12`0(78)` 8 Co`mmand l`i`ne` data

**Note:** The ACNMDBK field is an unnamed, reserved space.

<a id="HDRPWCC"></a>

Return Codes: The command processor for LOGON, AUTOLOG, and XAUTOLOG recognizes the following return codes: Any return code greater than X'16' is treated as X'08':

<a id="TBLTBLUNIQ213"></a>

| Return Code | Status |
| --- | --- |
| 0 | Request authorized |
| 4 | Request not verified, deferred to<br>CP |
| 8 | Request failed |
| 12 | User forced off the system16 |


<a id="SPTBEG"></a>

---

[Previous](7-12-1-5.md) | [Index](README.md) | [Next](7-12-1-5-2.md)
