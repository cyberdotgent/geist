[Previous](1-1-34.md) | [Index](README.md) | [Next](1-1-36.md)

---

## 1.1.35 Diagnose Code X'98'--Real I/O

**Privilege** **Class:** Any Using DIAGNOSE code X'98', a virtual machine can lock and unlock virtual

pages and can execute its own real channel programs. The four subfunctions of DIAGNOSE code X'98' are LOCK, UNLOCK, SIOF-Real, and SSCH-Real. The requested subfunction is identified by a code in the Rx register. The LOCK subfunction locks a single 4KB page of virtual machine storage in host real storage. In addition, it returns to the virtual machine the absolute storage address of the frame used to lock the guest page. The UNLOCK subfunction unlocks a single 4KB page of virtual machine storage that was previously locked by DIAGNOSE code X'98'. The SIOF-Real and SSCH-Real subfunctions initiate execution of a real channel program for 370 and ESA mode virtual machines, respectively. DIAGNOSE code X'98' allows a 370 or ESA virtual machine to bypass CCW translation and initiate execution of real channel programs. Execution of this DIAGNOSE bypasses most software and hardware protection mechanisms. For example, the virtual machine is able to read from, or write to, **any** frame in storage that does not have a storage key of 0. If the virtual machine is running a program with an error in it, system (that is, other users) security and integrity may be compromised. The ability to execute DIAGNOSE code X'98' is controlled by the directory OPTION statement, and should be granted only to the most trusted programs after careful consideration of the possible security and integrity exposures.

**LOCK** **Subfunction**

This subfunction locks in real storage a selected page of virtual machine absolute storage, thus excluding the page from future paging activity. Locking pages can enhance the efficiency of a particular virtual machine by keeping frequently-used pages in real storage. In 31-bit addressing mode, this subfunction operates exactly like the LOCK command, obtaining a real storage page from the normal paging area. In 24-bit addressing mode, a real page is obtained from a special area reserved for this function.

**Entry** **Values:**

Rx Contains X'00000000' the subfunction code Ry Contains the guest absolute address to be translated and locked: In 31-bit addressing mode, the guest address is in bits 0 through 31. In 24-bit addressing mode, the guest address is in bits 8 through 31; bits 0 through 7 must be zero.

**Exit** **Values:**

Ry+1 Contains one of the following: The host absolute address (set by CP with a condition code of 0) In 31-bit addressing mode, the host address is in bits 0 through 31. In 24-bit addressing mode, the host address is in bits 8 through 31. A return code (set by CP with condition code 3)

<a id="TBLTBLUNIQ27"></a>

| Return<br>Code | Meaning |
| --- | --- |
| 1 | The virtual machine is a V=R or V=F machine. |
| 2 | This is an invalid guest address.<br>3<br>There is no available real frame, or the page is already |


**UNLOCK** **Subfunction**

This subfunction unlocks a page of virtual machine storage that was previously locked by DIAGNOSE code X'98'. In 24-bit addressing mode, real storage pages become available for reuse by DIAGNOSE code X'98' LOCK requests. In 31-bit addressing mode, real storage pages become available for normal paging operations. **Entry** **Values:** Rx Contains X'00000004'-the subfunction code Ry Contains the guest absolute address of the page to be unlocked **Exit** **Values:** If UNLOCK was successful: CC=0 Ry+1 is unchanged. If UNLOCK was not successful: CC=3 Ry+1 contains one of the following return codes:

<a id="TBLTBLUNIQ28"></a>

| Return | Code |
| --- | --- |
| 2 | An invalid guest address |


**SIOF-Real** **Subfunction**

This subfunction initiates execution of a real channel program that the virtual machine has built in locked pages of storage. The address of the first CCW in the CAW is a host absolute address, as are the data addresses and transfer addresses (in TIC CCWs). This subfunction is valid for a 370 mode virtual machine, and it operates as a SIOF instruction. Condition codes 0, 1, and 2 are compatible with normal virtual I/O operation of that instruction. Condition code 3 is extended to provide a return code to indicate error conditions unique to the Real I/O interface. The device identified by the parameter register must be defined and dedicated. The device cannot be read/only. To protect system storage from overlay by guest real I/O, the subchannel key in the CAW must be non-zero. **Entry** **Values:** Rx Contains X'00000008'-the subfunction code Ry Contains the virtual device address (bits 16 through 31) **Exit** **Values:** Ry+1 Contains a return code (set by CP with condition code 3), as follows:

<a id="TBLTBLUNIQ29"></a>

| Return<br>Code | Meaning |
| --- | --- |
| 1 | The device is not operational or is not attached. |
| 2 | The device is not dedicated or is read-only. The virtual<br>machine is a V=F machine. |


**SSCH-Real** **Subfunction**

This subfunction initiates execution of a real channel program that the virtual machine has built in locked pages of storage. The address of the first CCW in the ORB is a host absolute address, as are the data addresses and transfer addresses (in TIC CCWs). Refer to IBM System/370 Principles of Operation or IBM System/370 Extended Architecture Principles of Operation for a description of ORB. This subfunction is valid for an ESA mode virtual machine, and it operates as a SSCH instruction. Condition codes 0, 1, and 2 are compatible with normal virtual I/O operation of that instruction. Condition code 3 is extended to provide a return code to indicate error conditions unique to the real I/O interface. The device identified by the parameter register must be defined and dedicated. The device cannot be read/only. To protect system storage from overlay by guest real I/O, the subchannel key in the ORB must be non-zero.

**Entry** **Values:**

R1 Contains the subsystem identification (SID) for the virtual device Rx Contains X'0000000C'-the subfunction code Ry Contains the guest logical address of ORB. **Exit** **Values:** Ry+1 Contains the return code (set by CP with condition code 3), as follows:

<a id="TBLTBLUNIQ30"></a>

| Return<br>Code | Meaning |
| --- | --- |
| 1 | The device is not operational or is not attached. |
| 2 | The device is not dedicated or is read-only. The virtual<br>machine is a V=F machine. |


You may receive one of the program checks shown in [Table 20](#TBL98PC) if the DIAGNOSE code X'98' input data is invalid:

<a id="98PC"></a>

Table 20. DIAGNOSE Code X'98'--Program Checks Encountered

| Encountered | Cause<br>Invalid entries in the ORB. |
| --- | --- |
| Operation exception | The user does not have the correct directory<br>authorization. |
| Specification exception | Any of the following:<br>Ry=R15, Rx=Ry or Rx=Ry+1<br>Unknown subfunction requested.<br>ORB |


**Usage** **Notes:**

1. If you attempt to lock a prefix page in a virtual multiprocessor configuration, an error code of 2 is returned. 2. If you attempt to lock a page that has been mapped with the DEFINE function of the MAPMDISK macro, an error code of 2 is returned. 3. In 31-bit addressing mode, the LOCK subfunction requires an available real storage frame in the area the system uses for paging. 4. In 24-bit addressing mode, the LOCK subfunction requires an available real storage frame in the reserved area defined by the RIO370 operand on the SYSSTORE macro instruction. 5. If you LOCK a page in a shared segment and do not issue an UNLOCK, the page is unlocked when the last user of the shared segment releases the storage at LOGOFF (or other command which clears storage). 6. The LOCK command issued for a user ID's virtual pages operates independently of the DIAGNOSE code X'98' page locking facility. Pages LOCKed by command cannot be unlocked by DIAGNOSE code X'98', nor can real I/O pages locked by DIAGNOSE code X'98' be UNLOCKed by command. 7. SIOF-Real and SSCH-Real do not support suspend/resume in channel programs. New I/O starts cannot be executed before previous I/O operations are concluded (early redrive is not supported). 8. At completion of an I/O operation started by DIAGNOSE code X'98', the CCW address returned to the virtual machine in the CSW on a 370 mode I/O interruption or in the IRB on an XA mode Test Subchannel instruction is a host absolute address. 9. The address-limit-checking facility of the Channel Subsystem is not available to users of the DIAGNOSE code X'98' SSCH-Real. The host absolute addresses used in the channel program are random addresses from the viewpoint of the virtual machine, so limit checking is meaningless. If the address-limit-checking control is set on in the ORB, it is ignored by the control program when scheduling the I/O. End-Use Note: If too many pages of real storage are locked, there may not be enough remaining frames to allow the system to operate efficiently. It is important for the system programmer to control the use of this facility.

---

[Previous](1-1-34.md) | [Index](README.md) | [Next](1-1-36.md)
