[Previous](3-1-2-1.md) | [Index](README.md) | [Next](3-1-2-3.md)

---

<!-- geist-render: severity=best-effort route=best-effort reason=typed-lowering-declined records=360-364 detail="prose topic rejected: cz flow dl carries display text" -->

### 3\.1\.2\.2 Type Definition Structure

<a id="HDRTYPDEF"></a>

```text
   FLOW P 3 3
   For each format specified on the #pragma mapinc directive, the compiler
   creates one or more C++ type definition structures (typedef of type
   struct), which describes the fields in the record format.  You indicate
   the type of fields to include in the typedef structures in your program by
   the option you select on the options parameter.
   FLOW P 3 3
   The type definition of type structure is:
          typedef struct {
                  .
                  .
                  .
          } LIBRARY_FILE_FORMAT_tag_t;
   FLOW P 3 3
   Parameters of the #pragma mapinc directive are used to create the name of
   the created type. LIBRARY, FILE, and FORMAT are the library_name,
   file_name, and format_name, respectively, that are, specified on the
   pragma. These names are folded to uppercase unless quoted names are used.
   The library and file names can be replaced by your own prefix_name as
   specified on the pragma.
   FLOW P 3 3
   If the prefix_name parameter is specified in the pragma, it is used in the
   first part of the generated typedef structure name in place of
   LIBRARY_FILE name.
   FLOW P 3 3
   Any characters not recognized as valid by the C++ language that appear in
   library and file names are translated to the underscore (_) character.  If
   you use the special characters @, #, or $ in a format name, those
   characters are changed to lowercase a, p, or d respectively.
   FLOW P 3 3
   The tag on the structure name indicates the type of fields included in the
   structure definition. The possible values for the tag are:
   FLOW DL 3 3
   Option    Tag
   FLOW DT 3 13
   input     i
   FLOW DT 3 13
   output    o
   FLOW DT 3 13
   both      both
   FLOW DT 3 13
   key       key
   FLOW DT 3 13
   indicators indic
   FLOW DT 3 13
   nullflds  nmap,nkmap
   FLOW DT 3 13
   lvlchk    lvlchk
   FLOW P 3 3
   If the option lvlchk is specified, the name of the array of the structure
   type created is _LVLCHK_T. This naming convention is not used for the
   other options listed above.
   FLOW P 3 3
   To include external file descriptions for more than one format, specify
   more than one format name (format1 format2) or (*ALL) on the #pragma
   mapinc directive. A header description and type definition structures are
   created for each format.
   FLOW P 3 3
   You can compile your C++ program with the compiler options /L+ and /Li+ to
   see the typedefs in your compiler listing. The type definitions are also
   generated in the listing if you specify the compiler options /L+ and /Lj+.
   You can save the external AS/400 file descriptions as a Windows file by
   using the compiler option /ASd+. You can then browse the external AS/400
   file descriptions and compile your source file in disconnected mode. See
   "Working in Disconnected Mode with External AS/400 File Descriptions" in
   topic 3.1.6 for more information about compiling in disconnected mode.
   FLOW H3 3 3
```

---

[Previous](3-1-2-1.md) | [Index](README.md) | [Next](3-1-2-3.md)
