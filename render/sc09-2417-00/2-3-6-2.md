[Previous](2-3-6-1.md) | [Index](README.md) | [Next](2-3-6-3.md)

---

## 2.3.6.2 Use Input and Output Feedback Information

<a id="HDRIOFEED"></a>

`_RIOFB_T` is a structure returned by most record I/O functions, that < contains feedback information on the last I/O operation. By default, the C ++ record I/O functions update any of the fields (except fo`r the num`_bytes */ f`ield) _R`IOFB_T after a record I/O operation is performed. If your program < does not use all these values, you can improve your program's performance by opening a file:

```text
and      fp = _Ropen("MY_LIB/MY_FILE", "rr, riofb = N");
```

& By specif`ying` r`i`o`f`b = N, only `the num_`bytes field, the number of bytes ++ read or written i`n the _R`IOFB_T structure is updated. If you sp`ecify` riofb `<`< = Y, all the fields i`n the _R`IOFB_T structure are updated.

---

[Previous](2-3-6-1.md) | [Index](README.md) | [Next](2-3-6-3.md)
