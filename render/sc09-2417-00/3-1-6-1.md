[Previous](3-1-6.md) | [Index](README.md) | [Next](3-1-7.md)

---

## 3.1.6.1 Compile Options When Working in Disconnected Mode

<a id="HDRCOMPDDS"></a>

Before working in disconnected mode, save the external AS/400 file descriptions on your Windows workstation. [Table 5](#TBLCOMDISC) describes the compile options you should consider to work in disconnected mode with include files generated by the #pragma mapinc directive.

<a id="COMDISC"></a>

Table 5. Compile Options for Disconnected Mode

| Option | Description<br>directives are ignored. The saved external AS/400<br>/ASdpath | /ASd-<br>in the disconnected mode, all #pragma mapinc<br>Save the external AS/400 file descriptions locally | Do not save the extern<br>you compile using this option in di |
| --- | --- | --- | --- |


Saving DDS Information Locally

Before you save the generated header file containing the record format typedefs locally, consider:

- 1. If a fully qualified path is specified in the include_name parameter in the #pragma mapinc directive, the external AS/400 file descriptions are saved there. A fully qualified path name is:

```text
CSELECT          #pragma mapinc("c:/abc/aaa.h","*LIBL/ABC(REC1)","input",...)
```

- If the source including this directive is compiled with the `/ASd-` compiler option, no external AS/400 file descriptions are saved.
- If the source including this directive is compiled with the `/ASd+` compiler option, the external AS/400 file descriptions are saved in `c:``\abc\aaa.h`.
- If the source including this directive is compiled with the `*/` /ASdpath compiler option, `(for e``xamp`le, /Asdc:\abc), the external AS/400 file descriptions are saved using the path name specified ] `for` the /Asdpath comp`il``er option,` c:\abc\aaa.h.
- < 2. If a relative path is specified in the include_name parameter, it is always relative to the current directory. A relative path name is:

```text
CHAR          #pragma mapinc("proj1/aaa.h","*LIBL/FILE(REC1)","input",...)
```

- If the source including this directive is compiled with the `/ASd-` compiler option, no external AS/400 file descriptions are saved.
- If the source including this directive is compiled with the `/ASd+` compiler option, the external AS/400 file descriptions are saved as `c:``\source\proj1\aaa.h`, assuming the current directory is `c:``\source`.
- If the source including this directive is compiled with the `*/` /ASdpath compiler option, `(for e``xampl`e, /ASdd:\appl), the external AS/400 file descriptions are saved using the path name specified / `for` the /ASdpath comp`il``er option, d:\app`l\proj1\aaa.h.
- 3. If the compiler opt`ion /ASd`path is used, the path specified by this compiler option is used. If a relative path is specified in the include_name parameter, it is always relative to this path. If that path itself is also a relative path, the current directory is used. If the source including this directive is compiled with the `/ASdpath` */ compiler option, (for `example,` /ASd\appl), the external AS/400 file / descriptions are `sa``ved as c:\appl\pr`oj1\aaa.h, assuming that the current directory is `c:``\appl`.

---

[Previous](3-1-6.md) | [Index](README.md) | [Next](3-1-7.md)
