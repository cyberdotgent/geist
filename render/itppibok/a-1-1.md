[Previous](a-1.md) | [Index](README.md) | [Next](a-1-2.md)

---

### A.1.1 User Considerations

The following list summarizes the language and command syntax changes, as <> well as other changes from TPNS Version 2 Release 4 to TPNS Version 3: If you use a TPNS communication controller, then you may need to modify your TPNS control program. Refer to the following list to learn about changed macros and operands:<BOOK> The CA and CHANTYP operands on the TPNSGEN macro have been deleted. The CSB macro has been deleted.<BOOK> The INTPRI operand on the TPNSxxxx macro has been deleted.) The ATTACH operand on the TPNSxxxx macro has been deleted. Use the CLOCKNG operand to specify how a line is attached to the communication controller. The TPNS115A and TPNS83B3 macros have been deleted.<> MODEL is now a required operand on the TPNSGEN macro.= The value of '3705' has been deleted from the MODEL operand on the TPNSGEN macro.= The value of '3745' has been added to the MODEL operand on the TPNSGEN macro.<BOOK> The SCANCTL and HSPDSEL operands have been deleted from the GENEND macro.

- The TRSPEED, TOKREL, and ADAPTER operands have been added to the TPNSLPRT macro.= Support of SYSDEF-less (Dynamic Routing Definition) Routing has been / changed. TPNS now allocates Virtual Routes (VRs) only when you code the definitions in your network definition statements.<> The following operands have been deleted from the NTWRK statement: - MULTROUT - LNCP about - ENA. The ENA=YES operand has been changed to LEVEL=ENA.* The LOGDSPY, COLOR, and ATRABORT operands will no longer accept the value of YES or NO. TPNS Version 3 Release 2 supports the simulation of multiple SSCP and NCP subareas using Extended Subarea Addressing or Extended Network ) Addressing (ENA). These subareas can coexist within the same network

<BOOK> <> <> < [SC31-6008> <> <TPNSDF>](#LNK) provided you follow the guidelines listed in Defining TPNS Networks.( Many of the TPNS control blocks have changed with Version 3. If you use user exit routines that directly reference fields in TPNS control blocks instead of calling the TPNS exit interface routine, you must reassemble your user exit routines using the DSECTs provided in the <> TPNS UEXINTFC data set. User exit routines that reference fields in TPNS control blocks by calling the TPNS exit interface routine should not require reassembly./ You must ensure that the TPNS channel end and start I/O appendages specified on the CHEAPP and SIOAPP TPNS execution parameters are the correct appendages for Version 3.: In TPNS Version 3 Release 2, you can enter line IDs as either 5-digit or 6-digit numbers. If you enter them as 5 digits, TPNS inserts a zero between the second and third digits. Line IDs are displayed and listed as 6 digits.( The TPNS log record format has changed with TPNS Version 3 Release 2. Consequently, you cannot use the Loglist Utility, the Response Time Utility, or the Log Compare Utility from Version 3 with log data sets created by an earlier level of TPNS.( If you use user exits that depend on statically allocated save areas, you must continue to code the SAVEAREA operand in the network definition. This ensures that TPNS will allocate the save areas required by the user exit when it initializes the network.

---

[Previous](a-1.md) | [Index](README.md) | [Next](a-1-2.md)
