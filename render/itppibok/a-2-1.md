[Previous](a-2.md) | [Index](README.md) | [Next](a-2-2.md)

---

### A.2.1 User Considerations

```text
   The following list summarizes the command and language syntax changes that <BOOK>    occur with TPNS Version 3 Release 2, as well as other changes that might
   affect TPNS users:

   ° You can now code the following new STL statements:<>
   ° -   VERIFY, to support the logging of VERIFY records in the log data,
   ° set                                                                       -   SETRH, to modify the request/response unit header
   ° -   SETTH, to modify the transmission header
   ° -   SNACMND, to support SNA commands.-
   ° You can now code the STL C2X function, to convert a string variable to
   ° its hexadecimal representation.

   ° The following names are now reserved words in STL:
```

<a id="TBLTBLUNIQ150"></a>

BB EOMI QUEC TGNFIFO BID EOMPB RELQ TGSWEEP BIS EOMS RESP UNBIND CDI EOT RIDISC VERIFY

| CEB | EXC | RNDISC | VRCWI |
| --- | --- | --- | --- |
| CHAIN | FMI | RSHUTD | VRCWRI |
| CHASE | FOR | RTR | VRPACCNT |
| CR | INITOTH1 | SBI | VRPRQ |
| C2X | INITOTH2 | SDT | VRPRS |
| DISPLAY | INITSLF1 | SEQNO | VRRWI |
| DR1 | INITSLF2 | SETRH | WRU |
| DR2 | LUSTAT | SETTH | XOFF |
| EB | NONE | SHUTD | XON |
| EDI | NTWKPRTY | SNACMND |  |
| EFI | PACE | SNI( |  |
| EOMC | QRI | TERMSLF1 |  |


Have TPNS statements always added to the sequential output data set. Coding NOTPNS before prevented this. - Have STL statements always added to the SYSPRINT output data set. Coding NOSOURCE before prevented this.* - Code integer expressions for the RANDOM and RNUM functions' low and high values.<> - Declare multiple variables on one BIT, INTEGER, or STRING statement.<BOOK> - Specify the format of the date output and request the date for a specified number of days in the future for the DATE function.<> - Have the COFF function, when coded in an assignment statement, <BOOK> return the offset relative to screen position 1. Before, coding the COFF function in an assignment statement returned the offset relative to screen position 0.<> You can now code two resource names on the following commands for the Log Compare Utility: - DEV - MSGTXT - LINE - LU - NODE - NTWRK - TERM - VTAMAPPL.: TPNS/ATP now supports the EQU and COPY statements in MFS source code./ You need to convert the VSAM screen definition file (SDF) to a format :H3 that allows a 16-byte key for the TPNS/ATP utility programs. You can

do this by running the VSAM IDCAMS job to convert the SDF file to the * new format. This JCL is supplied. Also, TPNS automatically converts = the TCF file from an 11-byte format to a 16-byte format whenever you

<BOOK> <> <> < [SC30-3453> <> <TPNSSG>](#LNK) access the file with the TPNS/ATP Editor. See TPNS Script Generating <BOO [K> <> <>](#LNK)<SC30-3453> <> <TPNSSG> Utilities for more information. PF6 is now assigned a function in ITPECHO.<> You can now code the EXTERM and EXDEV commands for the Loglist <BOOK> <> <> < [SC30-3290> <> <TPNSGU>](#LNK) Utility. See TPNS General Utilities for details of these commands. You can now code the EXTERM command for the Response Time Utility. <BOOK> < [> <> <SC30-3290> <> <T](#LNK) PNSGU> See TPNS General Utilities for details of this command.* BRANCH statements can now be executed within an execute deck. If you <> coded an execute deck and relied on the fact that the BRANCH statement acts as a delimiter and terminates processing of the deck, you must recode the deck. are You can now specify the BLOCK=(b,p) operand on the NTWRK, CNTLR, LINK, and PU (PU Type 4) statements to enable TPNS to send and receive blocked PIUs to and from type 4 PUs.<> The CNTRS operand on the NTWRK statement now specifies the minimum number of counters used for each line, terminal, and device when user <BOOK> exits are specified. When user exits are not specified, the CNTRS operand is ignored. This provides TPNS with the ability to allocate counters dynamically.<BOOK> You can now specify the LLS operand on the LINK statement to enable Token-Ring lines to be used on cross-domain links.<BOOK> You can now code counters for the random number specification on the <BOOK> <> <> [<SH20-2489> <> <TPNSLG>](#LNK) SET statement. See TPNS Language Reference for details of this change.<BOOK> You can now code counters for the random number specification on the <BOOK> <> <> [<SH20-2489> <> <TPNSLG>](#LNK) $RNUM$ data field option. See TPNS Language Reference for details of <> this change. This also applies to the LENG operand on the TEXT

before, coding onin index(substr(ru,5),'x')... *            would return the index starting at position 1 in the ru, not at <>            position 5.  now, coding this statement returns the index starting *            at position 5.  if your programs depend on the old value returned,                you may need to change your code.<>        -   displaying the screen image buffer by the statement log display. (            coding log screen or log buffer now logs the device buffer.(        -   code string constants as the first argument to the index function.                     -   perform string concatenation on the left side of an operator                      involving a non-constant string expression.)        -   code an expression without regard to the left and right side.  for <>            example, you can now code if 5=a then ....=        -   code do-end groups within onin, onout, and on signaled statements.                     -   include string constants or concatenation in the first argument ?            specified to utblscan..

You can now code D, TH, RH, RU, and (row,col) values for the $RECALL$ data field option.= You can now specify two new operands for the $DATE$ data field option. These allow you to specify the format of the date output and request the date for a specified number of days in the future.<> You can now specify the THROTTLE operand on the LU and DEV statement to support multiple message generation for LUs.<> Threshold values on the PATH statement were increased to 524287.* You can now specify a value of 4 for the DEF operand on the RTM statement.<> You can now specify the CPSESCAP and CPINIT operands on the PU21 statement to provide CP-CP session node support. You can now specify the CPSESS operand on the ADJNODE statement to indicate that the adjacent node has network node capabilities. / You can now specify the HWSUBFLD operand on the TERM, DEV, PU, and LU statements to support the Network Asset Management function.( EXTFUN=YES is now the default value for LU2. You can now specify the following user exit operands on the NTWRK statement: INEXIT OUTEXIT NCTLEXIT UCMDEXIT LNEREXIT UXOCEXIT. If you use a TPNS controller, you may need to modify your TPNS control <> program. The following list details the changed macros and operands:( You can now code ALCI for the TYPE operand of the TERM statement( You can now code ALCT for the TYPE operand of the DEV statement<> You can now specify the ALCSEOM operand on the MSGTXT and TEXT, statements You can now define ALC lines with the TPNSALC macro.

**Note:** The ALCI, ALCT, and ALCSEOM operands, along with the TPNSALC macro, are only available with the TPNS 3745 control program.<BOOK> If you are simulating multiple networks, local storage areas in user exits are no longer preserved across calls.* If you are logging messages to a disk data set on MVS, you no longer need to enter the E operator command twice.

---

[Previous](a-2.md) | [Index](README.md) | [Next](a-2-2.md)
