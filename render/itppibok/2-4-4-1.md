[Previous](2-4-4.md) | [Index](README.md) | [Next](2-4-4-2.md)

---

#### 2\.4\.4\.1 Single TPNS Session

When you are operating a single session of TPNS under MVS, you code the TNIAPPLS execution parameter on the JCL EXEC statement\. When you are operating TPNS under GCS, code it on the TPNS START statement in your GCS command file\.

The format of the TNIAPPLS execution parameter is the following:

`TNIAPPLS=(`*xxxxxxxx,yyyyyyyy*`)`

where *xxxxxxxx* is the name of the TPNS APPL and *yyyyyyyy* is the name of the NetView APPL\. You must specify both names and separate them with a comma\. Each name can be from 1 to 8 characters long\. The TPNS APPL name must be the name coded on the APPL statement that defines the TPNS side of the interface between the NetView program and TPNS to VTAM\. Likewise, the NetView APPL name must be the name coded on the APPL statement that defines the NetView side of the operator interface to VTAM\.

Using the APPL definitions in ["Defining the Operator Interface to VTAM,"](2-4-2.md) the following statements define to TPNS the interface between the NetView program and TPNS\. The following are example JCL statements:

`//GO EXEC PGM=ITPENTER // PARM='TNIAPPLS=(TPNSSIDE,NETVSIDE)'`

The following is a sample GCS command file statement:

`'TPNS START TNIAPPLS=(TPNSSIDE,NETVSIDE)'`

For more information about TPNS execution parameters, refer to [TPNS](<DOCNUM/SC30-3289/CCONTENTS>) [Operation](<DOCNUM/SC30-3289/CCONTENTS>)\.

---

[Previous](2-4-4.md) | [Index](README.md) | [Next](2-4-4-2.md)
