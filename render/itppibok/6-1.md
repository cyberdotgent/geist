[Previous](6-0.md) | [Index](README.md) | [Next](6-2.md)

---

## 6\.1 Naming the Network and Its Resources

<a id="HDRNETNAME"></a>

Before you code any network definition statements, you should become

familiar with all of the resources you want to simulate and how they are connected to each other and to the system under test\. You need to assign names to the network itself and to the resources you want TPNS to simulate\. You code these names when you define the network and its resources\. You use them when you need to refer to specific resources with other network definition statements or with TPNS operator commands\. The names can contain 1 to 8 alphanumeric characters\.

In general, you should assign a unique name to each resource in the network\. This makes it easier to refer to that resource later\. You must assign unique names to terminals to use some TPNS utilities, such as the Response Time Utility described in ["Using TPNS Output\."](9-0.md) In addition, you should try to use a consistent and meaningful naming convention throughout the network\. This helps you identify the location of the resource in the network\. For example, if the name of a line is 010020, you might name a simulated cluster controller on that line C201 \(for controller 1 on line 20\)\. Similarly, you might name two simulated terminals attached to the controller T2011 and T2012\. Finally, you may find it helpful to match the names in the simulated network to your real network\.

If you are simulating terminals using a TPNS control program, the name you assign to a line provides a link between the TPNS host program and the TPNS control program\. In this case, the name of the line to be simulated is its physical address\. The physical address is in the form *xxyyyy*, where *xx* identifies the communication controller to which the line is attached and *yyyy* is the relative line number within the communication controller\.

**Note:** The relative line number can be 3 or 4 digits\. If you specify a 5\-digit line address \(consisting of 2 digits for the controller and 3 digits for the relative line number\), TPNS will add a leading zero to the relative line number\. For example, if you specify a line address of 01123, TPNS uses the line with the address 010123\.

---

[Previous](6-0.md) | [Index](README.md) | [Next](6-2.md)
