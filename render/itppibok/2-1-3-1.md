[Previous](2-1-3.md) | [Index](README.md) | [Next](2-1-3-2.md)

---

#### 2.1.3.1 Real Input/Output (I/O) Requirements

<BOOK> SI input/output (I/O), real * SI VM, real input/output (I/O) requirements )     TPNS uses real I/O, if available, to communicate with the communication )    controllers used in the simulation.  Real I/O enables the TPNS virtual /    machine to fix (or lock) pages in storage and to run channel programs that <>    contain real addresses.  This eliminates the need for the channel programs               to be interpreted and translated by the control program, which in turn ?    improves TPNS performance.:    Whether you use real I/O when you run TPNS depends on your situation.  In :H3    most environments, using real I/O improves TPNS performance.  However, all =    pages that are locked for real I/O must be located in the first 16 Mb of *    real storage.  If demand for this storage is high, running TPNS with real :    I/O may degrade system performance.  If this occurs, run TPNS without real      I/O.:    To use real I/O, TPNS must run in a virtual machine that is associated *    with a user ID that has the DIAG98 option specified in its directory entry

TPNS uses real I/O, if available, to communicate with the communication controllers used in the simulation. Real I/O enables the TPNS virtual / machine to fix (or lock) pages in storage and to run channel programs that <> contain real addresses. This eliminates the need for the channel programs to be interpreted and translated by the control program, which in turn improves TPNS performance.: Whether you use real I/O when you run TPNS depends on your situation. In :H3 most environments, using real I/O improves TPNS performance. However, all pages that are locked for real I/O must be located in the first 16 Mb of real storage. If demand for this storage is high, running TPNS with real I/O may degrade system performance. If this occurs, run TPNS without real I/O.: To use real I/O, TPNS must run in a virtual machine that is associated with a user ID that has the DIAG98 option specified in its directory entry

/ and is authorized in the GCS group. Refe [r to "Authorizing TPNS o](2-3-3.md) n VM" for <> details about authorization. Note that authorization within the GCS group improves performance, even if you are not using real I/O.

---

[Previous](2-1-3.md) | [Index](README.md) | [Next](2-1-3-2.md)
