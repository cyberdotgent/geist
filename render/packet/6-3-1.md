### 6.3.1 Interfaces

The interfaces come first. These are as follows:

ASCII = serial port terminals with the XRouter command line

[FTNFTNUNIQ72](6-3-1.md)

ETHER = Ethernet LAN interfacing with libpcap (58)

HDLC = YAM modem interface

KISS = KISS TNCs or wireline links to other packet stacks

[FTNFTNUNIQ73](index.md)

MODEM = Hayes command set compatible dialup modems (59)

NETROM = NET/ROM backend interface or an old NET/ROM TNC

-

PPP = PPP (Point-to-Point Protocol) for IP links over serial cables

/

SLIP = SLIP (Serial Line IP) for Berkley UNIX-compatible serial

connections (started on UNIX using the slattach command)

TNC2 = TNC2 shell emulation on a serial por

Here is an example of one of each:

<a id="FTNFTNUNIQ72"></a>

(58) This requires that you downloaded a version of XRouter

built with libpcap support, and you must run it either as

root or give it the process capability for unfiltered network

and access: setcap 'CAP_NET_RAW+eip CAP_NET_ADMIN+eip' xrlin.

<a id="EFT"></a>

<a id="FTNFTNUNIQ73"></a>

(59) As in, modems that have commands that start with AT.

<a id="EFT"></a>

- Internal Loopback
- KISS Serial Port TNC
- WA8DED Host-Mode Serial Port TNC
- YAM Serial Port TNC
- NET/ROM Switch Serial Interface
- XRouter as a TNC2 Emulation
- XRouter Shell on Serial Terminal
- SLIP Serial Port Interface
- PPP Serial Port Interface
- Ethernet LAN Interface
- AXIP Tunnel Interface
- AXUDP Tunnel Interface
- AXTCP Server Interface
- AXTCP Client Interface
- AGWPE over TCP
